{
  "taskId": "integrate-docs",
  "status": "completed",
  "summary": "Closed the v9 run. (1) package.json gained 10 script lines: the two the plan named (\"verify:cortical-lobes\", \"verify:imaging-fit\"), the somatotopy check under both names (\"verify:somatotopy\" + the plan's bare \"somatotopy\" alias), \"verify:pip-contract\" (task 4's planned line, handed to this task), and five Node-tier gates that existed but had no npm line (\"verify:audit-checks\", \"verify:boundary-contract\", \"verify:a11y-contract\", \"verify:budget-report\", \"verify:closure-bite\"). Nothing pre-existing was renumbered or removed. (2) docs/SWARM_V9_PLAN.md gained section 8, the closure: §8.1-§8.5 what shipped per item with every number re-measured here (somatotopy table + ordering/pairing/residual evidence; the per-lobe share table with the per-plane absence list; the imaging residuals before/after and the improved/worsened counts; the PiP contract change and the 12 retired tokens), §8.6 what did NOT ship and why, §8.7 the full sweep with each gate's result and an explicit claims tier (non-browser verified vs orchestrator-browser-only). (3) README.md gained a full v9 section (five items + a consolidated honest-limits table + the gate table), a v9 intro/feature bullet set, corrected the two now-false v4 claims (the GPU PiP and the panel's real-slice backdrop), refreshed the content-scope counts (236 entries / 213 records / 190 structures / 23 tracts / 26 syndromes / 15 plates / 17 levels), the project layout and the script table, and added the tier-1b 'binding but RED' row. (4) docs/CONTENT_INVENTORY.md gained section 12 with the measured delta (220 -> 236 registry entries, 16 ids registered, 16 records), the full 16-row somatotopy placement table with measured normals and probe residuals, the lobe boundary method + residuals + per-plane shares + limits, and the imaging registration tables (CT/MRI per-plane, 24 applied plates before/after, 49 JPEG unmeasurable, 3 not-fittable). (5) NEW scripts/verify/pip-contract.mjs: the planned item-5 gate that task 4 did not create - 83 assertions in 5 groups over the shipped modules (panel structure, no real imagery, surviving chrome, clamp/persistence arithmetic, styles + sampler wiring), with its own measured limitation documented in the header (zustand 4 hands the static renderer the store's INITIAL snapshot, so the markup half asserts boot state and the other axes are proven through the same pure functions), and a bite check that re-applies 5 mutations in an isolated copy (.dsh-scratch/pip-bite) - 5/5 caught, restored copy re-running green, shared tree never written. SWEEP: npm run validate exit 0 (236 entries, 0 errors/0 warnings), check exit 0, build exit 0, verify:pipeline exit 0 (138/138 - 599204 tris - 386 loops), verify:plane exit 0 (10827), verify:somatotopy exit 0 (45/45), verify:cortical-lobes exit 0 (200/200), verify:pip-contract exit 0 (83). RED, reported not papered over: npm run verify:anatomy exits 1 before any verdict (spawnSync powershell EPERM -4048, sandbox); npm run verify:imaging-fit exits 1 at its own recompute step (spawnSync node EPERM) so it was driven through a byte-identical copy with only the transport swapped (5 insertions/9 deletions, git diff verified) which completes with 289 assertions and 20 failures = 18 real defects (16x manifests carry no registration.display.planes, 2x ubc-c13/c14 ship 3.108820 where the gate builds 3.10882) + 2 HEAD-path artifacts of that copy; verify:audit-checks exits 1 (90/1/7) on the pre-existing v8 'rows dimmed at default framing' check for the 14 vasc-* rows. Also measured and reported: the committed fitter run (dry-run capture + --report, 292 s) shows CT and MRI display corrections REJECTED (applied:false, mean centroid residual 18.09 -> 20.39 au and 9.40 -> 17.75 au, 2/8 and 3/8 planes improving) while 24/24 measurable PNG plates are applied (mean ROI IoU 0.0741 -> 0.4468, 0 worsened) and 49 JPEG plates stay unmeasurable, with ct.bin/mri-t1.bin and both grids' dims/originAu/spacingAu byte-identical to HEAD. Write scope respected: git status shows this task modified only README.md, docs/CONTENT_INVENTORY.md, docs/SWARM_V9_PLAN.md, package.json and added scripts/verify/pip-contract.mjs (plus gitignored .dsh-scratch scratch); every other modified file predates this task. Known gap reported rather than fixed (task 5's file): scripts/verify/audit-checks.test.mjs:1155 still asserts the retired /drawImage|backdrop/ backdrop and passes only because the removal is documented in SectionPiP.tsx's header."
}
