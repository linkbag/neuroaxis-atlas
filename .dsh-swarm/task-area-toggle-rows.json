{
  "taskId": "area-toggle-rows",
  "status": "completed",
  "summary": "v11 §1 shipped: the header's preset row is replaced as the primary control by TWO labelled toggle rows — 'Areas' (Telencephalon 85 rows · Diencephalon 39 · Mesencephalon (midbrain) 25 · Metencephalon (pons + cerebellum) 40 · Myelencephalon (medulla) 33 · Cerebral vasculature 14 = 236 = every taxonomy entry; 7/7 regions owned exactly once) and 'Systems' (Nuclei 88 · Tracts 53 · Ventricles 11 · Surface 25 · Context 45 · Vessels 14 = 236 over ALL_KINDS). Both filter the 3D scene, the 2D live section and the PiP through the store's existing layers.regions / layers.kinds (wrappers over toggleRegionLayer / toggleKindLayer; no new layer field, no new storage key). Every control is a real <button type=button> with aria-pressed, an accessible name whose visible text is a prefix (WCAG 2.5.3) and a machine hook: data-area (+ data-division) on the areas row, data-kind on the systems row, data-preset on the preset buttons, data-header-action on Reset/All. FILES: src/state/store.ts (new AREAS table DERIVED from DIVISIONS — metencephalon = rhombencephalon minus medulla, myelencephalon = medulla; AreaId/AreaDefinition; areaRegions/areasOf/areaLayersOn; ALL_ON_LAYERS = layersFromPreset('all'); a load-time partition assertion block placed AFTER the v10 division block so verify:division-toggles' load-time bite still names the DIVISION defect), src/components/Header.tsx (two rows + Reset/All; preset row kept as the shortcut row, presented last via flex `order` while staying FIRST in the DOM so audit.mjs:995's '[button] text === Nuclei' still resolves to the preset), src/components/Legend.tsx (documentation + each division's title now names the header areas it resolves to; the v10 division control and every row kept), scripts/verify/area-toggles.mjs (NEW, 331 assertions / 0 failed). PRESET STATE: VIEW_PRESETS + viewPresetOf untouched and still asserted; Reset calls the existing applyViewPreset('brainstem-focus') and All resolves through viewPresetOf(ALL_ON_LAYERS) ?? 'all', so there is no second copy of the default rule; DEFAULT_LAYERS and viewPresetOf(DEFAULT_LAYERS)==='brainstem-focus' unchanged (32 hidden ids, vasculature region off, all 6 kinds on; area row boots true,true,true,true,true,false). EVIDENCE (all exit 0): `npm run check` 0; `node scripts/verify/area-toggles.mjs` 331 passed / 0 failed (prints the partition table: per-button regions, taxonomy rows, division, boot state; proves total+disjoint partitions, derivation of AREAS from DIVISIONS and equality with the region sets parsed out of the store's own source text, per-area OFF→ON/ON→OFF adding/removing exactly its regions with kinds/hidden/emphasis set-equal, Reset reproducing the default exactly from a dirty state and after All, the rendered <Header/> contract incl. the one forbidden exact label 'Vasculature', a child-process wiring probe that fires every real onClick, and TWO load-time mutations of the AREAS table that must exit non-zero and name the defect); `npm run verify:audit-checks` 92 passed / 0 failed; `npm run verify:closure-bite` 7/7 mutations caught + restored copy re-runs green 92/0. Regression sweep also green: verify:pipeline, verify:plane 10827, verify:plane-helper-extent 196/0, verify:somatotopy 45/0, verify:pip-contract 187/0, verify:boundary-contract 22/0, verify:a11y-contract 38/0, verify:budget-report, verify:division-toggles 250/0. NOT OBSERVED (Chrome cannot start in this sandbox, so this task never claims them): verify:acceptance / verify:audit — what a user SEES (the two rows rendering, a toggle changing the 3D scene or the canvas pixels, real keyboard/pointer use) is orchestrator-lane only; this lane asserts the rendered DOM contract, the accessible names and the wired handlers. verify:anatomy and verify:imaging-fit are red at base for an environmental reason (spawnSync EPERM) and are in no task's write scope. verify:cortical-lobes currently exits 1 with 559/560 ('AXIS_PAIR.z order') from the PARALLEL section-correctness task's in-flight edits to scripts/verify/cortical-lobes.mjs — that file and the plane-geometry sources it reads are outside this task's write scope and untouched by it. package.json was NOT edited: the integrator should add `\"verify:area-toggles\": \"node scripts/verify/area-toggles.mjs\"`. Note: the new area partition rule lives only in store.ts (its own load-time block + area-toggles.mjs), because scripts/verify/audit-checks.test.mjs is outside this task's write scope and the rule is additive — the mirrored DEFAULT-framing assertion is byte-for-byte unchanged and still green."
}
