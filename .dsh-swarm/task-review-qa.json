{
  "taskId": "review-qa",
  "status": "completed",
  "summary": "v9 adversarial review of items 1-5 + the browser checks re-pointed at the new contracts. WRITE: scripts/verify/audit.mjs only (+852/-31, 137,892 B). VERIFIED HERE (exit 0): node --check scripts/verify/audit.mjs; npm run check; npm run validate (0 errors/0 warnings); npm run build; npm run verify:pipeline (138/138 parts, 599,204 tris, 386 loops, 0 problems); npm run verify:plane (10,827 assertions); node scripts/verify/somatotopy.mjs 45/45; node scripts/verify/cortical-lobes.mjs 200/200; node .dsh-scratch/qa/scan-audit-snippets.mjs (280 page-side snippets, all 107 code literals parse). FALSIFICATION RESULTS: (1) somatotopy placement is genuinely probed - re-running .dsh-scratch/somatotopy-probe7b.mjs reproduces the 16 committed origin3d/normal/arcS/residual rows exactly, GAP: scripts/verify/somatotopy.mjs asserts arcS monotonicity on the committed column without recomputing it (I recomputed cumulative chord length from the committed surfacePoints: exact on M1, within 0.02 au on 4 of 8 S1 values, so the column is real geometry, not a hand-edit); x monotonicity IS geometry-asserted; the 3D body-part labels render only for the hovered/selected segment (2 .label3d nodes per segment, one per hemisphere) so at rest the map reads by colour ramp only. (2) The lobe partition is total (classifyCorticalPoint's final fallback is 'parietal'), never assigns outside the ribbon and per-plane shares sum to 100.0%; the plan's 'each division non-empty on the reference planes' holds only in the weak reading: insula is absent on 10 of 13 reference planes, limbic on 4, occipital on 3, and y=-46/-24/-8 miss the ribbon entirely (its limit is y=-6.803). (3) HEADLINE: the CT mis-registration the user reported is NOT fixed - the fitted correction was measured and REJECTED (manifests carry applied:false; CT mean centroid residual 18.09 au = 21.7 mm, max 51.54 au = 61.9 mm; MRI 9.40 au = 11.3 mm, max 30.97 au; the chosen single similarity would have made 6/8 planes worse, 18.09 -> 20.39 au), while the photo/stain half IS materially improved (7 of 24 measurable plates applied: IoU 0.09 -> 0.62-0.72, residual 46.6 -> 1.7-4.8 au; 17 kept with per-plate reasons; 49 JPEG plates unmeasurable - no decoder). Recorded residuals DO match a fresh recomputation (I re-ran node scripts/fit-imaging-affine.mjs --json, exit 0, and compared the manifest numbers exactly); NO plane got worse (0 of 8 in both tables; the 17 kept plates were kept because their optimum sat on the search bound); the MRI/CT frozen invariant is honestly intact - the only manifest change is the additive registration.display block (dims/originAu/spacingAu/intensity/windows/status/canonicalBox/patientLeft are byte-identical to HEAD) and ct.bin/mri-t1.bin are unmodified. scripts/verify/imaging-fit.mjs and npm run verify:anatomy exit 1 IN THIS SANDBOX ONLY (spawnSync node/powershell EPERM, errno -4048) - not executed, not claimed. (4) images-off is a hard short-circuit in the one sampling place (resolveSliceModality returns {modality:'none'} for 'none'), the state line/aria-label/hint all come from one store string, persistence is neuroaxis.sectionUnderlay.kind='none', and no credit line is drawn - plus the panel forces the same state through a reference-counted scope that never persists. (5) The GPU stencil rig is really gone (grep for createPipRig/disposePipRig/supportsMsaaTargets/OWN_*_PLANES/PARITY_*/pipContextState/pipdebug/stencil/WebGLRenderTarget finds NO match in any module; only stale prose remains in src/geometry/materials.ts), the panel is DOM + one 2D canvas with a drawImage/putImageData guard covering all 5 imagery blit sites in imageLayers.ts, the size persists (neuroaxis.sectionPipSize), hide/restore keeps the chosen size, and the main-canvas context-loss checks are untouched. FINDING (dead code): firstSectionCapColor()/registeredAnatomyMaterials() now have NO caller anywhere and materials.ts:109 still documents the registry as read by the retired PiP - materials.ts is outside every v9 task's write scope, so it is a hand-off for the integrator. FINDING (docs, task 6 pending): README.md:12 still documents the retired GPU PiP and 'real imagery as the base layer'. FINDING (process): src/data/sectionImages.ts, src/data/load.ts and scripts/apply-plate-fits.mjs were changed by the orchestrator's rescue of imaging-registration, i.e. outside the plan section 6 write table, and .dsh-swarm/task-imaging-registration.json was never written. AUDIT.MJS CHANGES - nothing deleted, 3 checks repointed (L5b .pip-backdrop-hint now asserts the coverage number + 'this panel shows the simulated section' + an imagery-free panel; block M's PiP context-loss note now asserts the retired note is ABSENT and the panel's 2D canvas is still painted through the main canvas' loss cycle; the boot PiP line now asserts the panel's structure) and 6 groups added (A1 panel structure at boot; axis badges per axis + readout; B3 resizable/persisted/preset/hide+restore; P1 somatotopy tree order + record resolution + overlay label + region/kind layer gates; P2 lobe toggle + legend + repaint hash + persistence; P3 images-off on the Plates surface; P4 panel independence from the Plates modality + no persisted rewrite + reload persistence of size/layer/imagery). BROWSER LANE NOT RUN: Chrome is denied in this sandbox, so npm run verify:audit / verify:acceptance are the orchestrator's, and every behaviour claim above that requires a page is marked orchestrator-verified only."
}
