{
  "taskId": "review-qa",
  "status": "completed",
  "summary": "v11 review + re-point. FILES: scripts/verify/audit.mjs (+1260/-36) and scripts/verify/checks.mjs (+247/-5); scratch evidence (gitignored) .dsh-scratch/review-qa-rule-probe.mjs, review-qa-predicate-probe.mjs, review-qa-predicate-probe2.mjs. NO check deleted (the only re-purposing: ARTEFACT_PLANES[].drawn stops being the pass condition and becomes a printed cross-check, as PLAN 3b requires). (1) RE-POINTED all four preset-label click sites: audit.mjs:2328 and :2432 clickText('Brainstem focus') -> clickHook('[data-header-action=\"reset\"]') + assert data-preset=\"brainstem-focus\" aria-pressed=true at the same moment; :2353 clickText('Vasculature') -> the Areas row ([data-area=\"vasculature\"], ON and OFF, asserting the tree, the legend and the button's own aria-pressed) + the preset addressed by [data-preset=\"vasculature\"] + a new composition check (Areas{vasculature}=on plus Systems{nucleus,tract,ventricle}=off must give the preset's three readings); :995 the ambiguous textContent==='Nuclei' lookup -> [data-kind=\"nucleus\"], with the legend's own 'nucleus' checkbox asserted to follow and the two colliding 'Nuclei' buttons printed; the boot read now separates .header-presets[data-preset] buttons from the two new v11 actions. checks.mjs presetFocusReading decides on bootActivePresets (machine hook) when the reading carries it and keeps the label fallback, so the audit-checks mirror's fixtures are unchanged (92/0). 'Cortex only'/'Whole brain'/'Deep structures' are clicked NOWHERE (grep: prose only) - no phantom fix was invented. (2) NEW CONTRACT CHECKS: checks.mjs headerToggleRowsReading - 10 pure verdicts (both rows present with role=group + distinct accessible group names; hooks total over the shipped AREAS/ALL_KINDS; real <button type=button> with boolean aria-pressed and an accessible name starting with the visible text; areas partition total+disjoint over ALL_REGIONS; systems == ALL_KINDS; every aria-pressed the SAME fact as the legend's layer set; Reset pressed exactly when the documented default preset is; the preset shortcut row still real aria-pressed buttons; no exact label shared by a hooked and an unhooked control; the exact text 'Vasculature' carried by at most one hooked control) - driven from the real DOM at a clean boot (block A0b) and after Reset (block R1). NEW audit block R (R1-R6): toggles the telencephalon AREA and the nucleus/context SYSTEMS and measures the 3D mesh multiset (scene re-bound to the newest mounted scene after each tab round trip), the 3D canvas' OWN screenshot rectangle (regionPixelStats - a whole-page hash would change with the button's own class), the PiP's 2D canvas and the Plates live-section canvas (hash + painted counts), asserts the cortical-division legend EMPTIES on BOTH surfaces with the area off (the PLAN 2 ribbon gate), the tree dims the region, requires the button path and the legend per-region/per-kind checkbox path to render identical mesh names + an identical Plates hash, and requires Reset to reproduce BOTH the clean-boot pressed set and the default render. (3) CARRY-OVER 3a: the audit reads the ORDERED AXIS_PAIR/AXIS_INDEX out of section/planeGeometry.ts and the hand-typed AXIS_INDEX table is gone; proven EXECUTING - `node scripts/verify/audit.mjs` prints 'v11 source facts: ... AXIS_PAIR {\"y\":[\"x\",\"z\"],\"x\":[\"z\",\"y\"],\"z\":[\"x\",\"y\"]} - AXIS_INDEX {\"x\":0,\"y\":1,\"z\":2}' before exiting 4, and verify:plane-helper-extent lane A2 flipped from 'ascending-name in-plane derivation PRESENT' to 'absent - reads AXIS_PAIR yes'; the rendered sagittal quad is measured 148.00 x 171.00 = AXIS_PAIR.x=[z,y] extents, so the audit now agrees with the helper instead of contradicting it. CARRY-OVER 3b: the rule set is re-derived AT AUDIT RUNTIME (module.register + Node-24 type stripping importing the shipped corticalRunsForLoop/paintedDivisionsOfLoops and contours.extractContours, floors 10/25/25 au read from the source, over BOTH ribbons named by SECTION_CORTICAL_RIBBON_SLUGS) and compared to the canvas' own .section-lobes-legend rows, printing rule set || canvas set plus the per-ribbon split; independently reproduced by .dsh-scratch/review-qa-rule-probe.mjs: 6/6 artefact planes reconcile with the table (y=6 rule(l+r)=[frontal parietal temporal occipital limbic] vs left-only 4; y=14 l+r=all six vs left-only 4) - the spec's 'the canvas paints NONE' premise is retired with numbers, at exactly the plane that produced it. (4) EVIDENCE, all exit 0: npm run check; npm run validate; node --check scripts/verify/audit.mjs; node --check scripts/verify/checks.mjs; npm run build; verify:pipeline (138/138 parts, 599204 tris, 386 loops, 0 problems); verify:plane (10827 assertions); verify:plane-helper-extent (206/0); verify:division-toggles (250/0); verify:pip-contract (187/0); verify:somatotopy (45/0); verify:cortical-lobes (564/564); verify:audit-checks (92/0); verify:closure-bite (7/7 mutations caught); verify:boundary-contract (22/0); verify:a11y-contract (38/0); verify:budget-report (PASS); scripts/build-anatomy-geometry.mjs --manifest (PASS); scripts/verify-imaging-v4.mjs and -v4b.mjs (OK); node scripts/verify/area-toggles.mjs (331/0); node scripts/verify/view-filter-consistency.mjs (100/100). Predicate falsification (.dsh-scratch/review-qa-predicate-probe2.mjs): the real header shape (12 toggles, 9 presets, Reset/All, Quality, References) -> 10/10 ok, and 10 mutations each fail exactly the intended claim (row missing, hook dropped, aria-pressed removed, pressed-vs-layer disagreement, broken partition, name without the visible-text prefix, preset row deleted, Reset unbound, hook-free duplicate label, Vasculature renamed - the last one stays green on purpose). COULD NOT OBSERVE - Chrome cannot start here (crashpad OpenProcess Access is denied (0x5); the audit exits 4 before any check runs): verify:audit, verify:acceptance and verify:browser are the ORCHESTRATOR's lane, and every claim about what a user SEES (the two rows rendering, pixels repainting, keyboard activation) rests on code I could not execute; it is written to FAIL rather than pass by not running (probe missing/null, scene bridge unbound, rule not derivable, legend row missing all produce bad(), not a skip). verify:anatomy and verify:imaging-fit are red at base for the same environmental reason (spawnSync powershell/node EPERM) and are unchanged by v11. FINDINGS: (a) the preset ROW is kept, visibly demoted below Areas/Systems (DOM order presets-first for the boot assertion, flex order 0/1/2 for the user) - after my re-point the ONLY check that still depends on the row is the boot-preset reading; (b) two header buttons read exactly 'Nuclei' (preset + system toggle) and two read 'All' (preset + action, both wrapping VIEW_PRESETS.all) - both hooked, and a new claim fails if an UNHOOKED duplicate ever appears; (c) 'npm run verify:area-toggles' and 'npm run verify:view-filter-consistency' EXIT 1 with 'Missing script' - the integrator must add both lines to package.json (both gates are green when run directly); (d) headerToggleRowsReading is not mirrored in audit-checks.test.mjs (outside every v11 write scope) - its Node-lane coverage is verify:area-toggles, and it was falsified here with 10 mutations; (e) the audit now imports shipped .ts modules in Node (same technique as verify:cortical-lobes) - if that ever fails the audit reports a FAILURE, never a pass."
}
