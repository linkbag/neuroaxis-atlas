{
  "taskId": "architect-review",
  "status": "completed",
  "summary": "Wrote PLAN.md (394 lines, 32685 bytes) at the workspace root: the refined executable contract for SWARM v10, every interface claim verified against source at f5d3ed2. Verified and stated with numbers: (1) PlaneHelpers.tsx quads span x[-48,48] z[-41,41] y[-55,45] against CLIP_BOUNDS x[-58,58] y[-55,116] z[-76,72] — measured 68.7 au of cortex above the helper / 71 au missing at its top, 35+31 au missing in z, 10+10 au in x, and the gridHelper disagrees with its own quad; rule + acceptance (a pure exported planeHelperGeometry and a clipPlanes.ts mutation the check must bite on) specified. (2) Division control: the exact store shape (DIVISIONS / divisionsOf / applyDivisionLayers / soloDivisionLayers + two thin actions), no new AtlasLayers field, no change to DEFAULT_LAYERS / VIEW_PRESETS / initialLayers and no persistence key; risk (a) answered — viewPresetOf compares regions/kinds/hidden/emphasis only, so the load-time blocks (store.ts:821, :924) and the audit-checks mirror (which reads DEFAULT_LAYERS at import, never after a click) cannot break, while the real hazard is closure-bite.mjs:145,154 exact-string anchors in store.ts; solo dims the tree through the existing layerOff(), so TaxonomyTree.tsx needs no edit. (3) PiP: the pipsizeFromCornerDrag formula and PIP_CORNER_EDGES specified; .pip-resizer kept on all four handles so audit.mjs:505/523/661/671 and the keyboard block :679-696 keep passing, first handle deterministic, every handle's aria-label carrying the live WxH size. (4) Item 4 reproduced with three probes (arc length, shoelace drawn area, canonical/canvas positions): 417 runs over 43 planes — five 1-vertex runs of 0.00 au from the off-by-one at splitRuns' first range (corticalLobes.ts:383-394), 74 runs below 10 au, and long thin wedges where a division exists ONLY as under 10 au^2 (y=30 insula 21.6 au arc / 0.55 au^2; y=32 temporal 65.5 au / 5.50 au^2; limbic 1.2-9.5 au^2 at y=8..16); MIN_DIVISION_RUN_AU=10 + MIN_DIVISION_AREA_AU2=10 + MIN_DIVISION_LABEL_AREA_AU2=25, with the label-anchor competition moved from vertex count to painted area. (5) Item 5: no standing context-label pass exists — the name reaches the canvas only through drawSelectedLabel/drawHoverLabel, so NO_CANVAS_LABEL_RECORD_IDS must gate both plus the .section-structure-chip; the three authored plate SVGs carry their own cortex label and are outside every write scope — flagged, not silently fixed. Baseline gates run in this task: validate/check/pipeline/plane(10827)/somatotopy(45/45)/cortical-lobes(200/200)/pip-contract/audit-checks/closure-bite/boundary-contract(22/22)/a11y-contract(38/38)/budget-report/imaging-v4/imaging-v4b all exit 0; verify:anatomy and verify:imaging-fit are exit 1 AT BASE (piped child stdio EPERM; imaging-fit also RED at v9 close-out) — reported RED-and-unchanged, never claimed. Section 10 numbers ten places where docs/SWARM_V10_PLAN.md is wrong or under-specified. NOTE: PLAN.md is the plan-of-record path, so the previous v9 refinement was overwritten; it was tracked by neither git nor any docs/ archive (the v9 spec only names the path, docs/SWARM_V9_PLAN.md:176), the v10 contract needed that path, and the v9 worktree text is not recoverable. Wrote PLAN.md only; the probes and their captured outputs remain in .dsh-scratch/v10-arch/ (gitignored)."
}