{
  "taskId": "section-correctness",
  "status": "completed",
  "summary": "§2 ONE VISIBILITY DECISION + §3 CARRY-OVER, all gates green. Sources: (a) src/components/section/SectionCanvas.tsx — isPartVisible/isPart createLobeLayer/buildLobeLayer exported, RenderOrderCache.visibleLayers records the layer snapshot the visible list was filtered with, buildLobeLayer now re-applies isPartVisible (the ONE path that bypassed the layer sets: it was correct only because its input was pre-filtered), and it splits with the shared rule corticalRunsForLoop; (b) src/components/section/corticalLobes.ts — corticalRunsForLoop (splitter + drawn-path minimum = the ONE rule the canvas, the legend and both gates consume) + paintedDivisionsOfLoops; (c) src/components/viewer3d/SceneLayers.tsx — exported layersAdmit/isStructureVisible/isTractVisible/isEnvelopeSlotVisible/isGhostShellVisible + ENVELOPE_SLOTS/ENVELOPE_RECORD_IDS; every 3D pass consumes them and the component body holds ZERO direct region/kind reads; (d) src/components/viewer3d/PlaneHelpers.tsx — documents the ordered-AXIS_PAIR convention (no behaviour change; the quad was already right); (e) scripts/verify/view-filter-consistency.mjs NEW (100/100, exit 0): sweeps 138 parts / 213 structures / 23 tracts / 10 envelope slots / 2 ghost shells over 7 areas × 6 systems and 4 states each (548 cross-surface comparisons, 0 disagreements), prints what each surface hides per area/per system, executes the shipped buildLobeLayer over both committed ribbon GLBs (parity 5/5 planes; area off ⇒ 0 ribbons/0 divisions even with the UNFILTERED catalogue), asserts isPartVisible is the only layer read in the canvas, and BITES: a scratch copy with the gate line removed paints 5 divisions with the telencephalon off and a canvas-private rule filter diverges from the shared rule (both caught, SectionCanvas.tsx sha-256 unchanged before/after). §3a: the sagittal quad 148×171 is RIGHT (u = AXIS_PAIR.x[0] = z → 72−(−76) = 148; v = y → 116−(−55) = 171); the AUDIT's derivation ['x','y','z'].filter(c => c !== axis) is ascending axis NAME = ['y','z'] → 171×148, i.e. the wrong side — fix belongs to scripts/verify/audit.mjs (task review-qa), reported by plane-helper-extent lane A2 which reads AXIS_PAIR out of planeGeometry.ts and asserts quad, axisExtents (2D canvas/PiP camera/backdrop sampler) and the section plane frame all use the one ORDERED table. §3b: the spec's 'canvas paints NONE at y=6/y=14' premise is retired with numbers — the canvas paints BOTH ribbons, so y=14 gives rule(l)=[frontal temporal occipital insula] vs rule(l+r)=[frontal parietal temporal occipital insula limbic] and y=6 gives 4 vs 5 (parietal/limbic come from the right ribbon); the audit's ARTEFACT_PLANES table (read from its source, never retyped) reconciles 6/6 rows with the measured two-ribbon rule, the floors are read out of corticalLobes.ts (10/25/25 au; the v10 plan's '10 au²' is stale), and parity canvas≡rule is asserted at 5/5 planes. NEW DEFECT FOUND AND FIXED (not in the brief, exposed by the new convention lane): corticalLobes.planePointToCanonical mapped the CORONAL axis down the transverse branch ([u, planeValue, v] → plane value on y, loop v on z), so every coronal section was classified at wrong canonical coordinates — measured on ctx-hemisphere-l, before → after: z=0 [parietal temporal limbic] → [frontal temporal insula limbic], z=30 [insula parietal] → [temporal insula frontal parietal], z=40 [parietal limbic] → [frontal parietal], z=−30 [parietal temporal limbic] → [frontal temporal]; now [u, v, planeValue], asserted for all three axes by both gates. Gates run with numbers: npm run check 0; node scripts/verify/view-filter-consistency.mjs 100/100 exit 0; npm run verify:cortical-lobes 564/564 exit 0; npm run verify:plane-helper-extent 206 passed/0 failed exit 0; npm run verify:pipeline PASS (138/138 parts, 599204 tris, 386 loops, 0 problems) exit 0; npm run verify:plane 10827 assertions PASS; verify:pip-contract 187/0; verify:somatotopy 45/0; verify:audit-checks 92/0 (7 informational); verify:boundary-contract 22/0; verify:a11y-contract 38/0; verify:closure-bite 7/7 mutations caught, restored copy re-runs 92/0; verify:budget-report PASS; verify:division-toggles 250/0; npm run validate 0 errors/0 warnings. package.json NOT touched — integrator line requested: \"verify:view-filter-consistency\": \"node scripts/verify/view-filter-consistency.mjs\". COULD NOT OBSERVE (no Chrome in this sandbox): verify:acceptance, verify:audit, and anything about what a user SEES (toggle buttons rendering, pixels changing, keyboard order) — orchestrator lane; verify:anatomy and verify:imaging-fit are red at base for an environmental reason (spawnSync powershell/node EPERM) and were not touched. Honest residual: the 3D surface also honours the preset's structure-level `hidden` set (28 records under brainstem-focus) which the 2D section does not — a different axis from areas/systems, measured and printed in lane C2 rather than silently unified."
}
